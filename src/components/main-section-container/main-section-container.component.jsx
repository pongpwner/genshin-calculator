import React from "react";
import "./main-section-container.styles.scss";

export const MainSectionContainer = ({ children }) => (
  <div className="main-section-container">{children}</div>
);

export default MainSectionContainer;
